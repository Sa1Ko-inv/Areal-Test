import React from "react";
import {observer} from "mobx-react-lite";

const App = observer(() => {

    return (
        <div>
            Welcome to the App component!
        </div>
    );
});

export default App;
